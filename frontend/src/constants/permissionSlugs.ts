/** Default permission slugs (aligned with backend migration seed). */
export const ALL_PERMISSION_SLUGS: readonly string[] = [
  "student_import.run",
  "student.bulk_suspend",
  "student.bulk_delete",
  "suspended.priority",
  "policy.write",
  "audit.export",
  "roles.view",
  "roles.manage",
  "users.manage",
];
